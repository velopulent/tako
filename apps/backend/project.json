{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "backend",
  "projectType": "application",
  "sourceRoot": "apps/backend",
  "implicitDependencies": ["dashboard"],
  "tags": ["type:app", "scope:backend"],
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "../../tools/tako-go.sh run ./cmd/tako serve --dev",
        "cwd": "apps/backend",
        "env": {
          "GOCACHE": "/tmp/tako-go-cache",
          "TAKO_DASHBOARD_DIR": "",
          "TAKO_BRANDING_DIR": "packaging/branding"
        }
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "cache": true,
      "inputs": ["production", "^production"],
      "outputs": ["{workspaceRoot}/bin/tako"],
      "options": {
        "commands": [
          "mkdir -p ../../bin",
          "../../tools/tako-go.sh build -buildvcs=false -trimpath -ldflags='-s -w' -o ../../bin/tako ./cmd/tako"
        ],
        "cwd": "apps/backend",
        "env": {
          "GOCACHE": "/tmp/tako-go-cache",
          "TAKO_DASHBOARD_DIR": ""
        },
        "parallel": false
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": ["default", "^production"],
      "options": {
        "command": "../../tools/tako-go.sh test ./...",
        "cwd": "apps/backend",
        "env": {
          "GOCACHE": "/tmp/tako-go-cache",
          "TAKO_DASHBOARD_DIR": ""
        }
      }
    },
    "race": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": ["default", "^production"],
      "options": {
        "command": "../../tools/tako-go.sh test -race ./...",
        "cwd": "apps/backend",
        "env": {
          "GOCACHE": "/tmp/tako-go-cache",
          "TAKO_DASHBOARD_DIR": ""
        }
      }
    },
    "integration": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "command": "../../tools/tako-go.sh test ./...",
        "cwd": "apps/backend",
        "env": {
          "GOCACHE": "/tmp/tako-go-integration-cache",
          "TAKO_DASHBOARD_DIR": "",
          "TAKO_EXTRA_GO_TAGS": "integration"
        }
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": ["default", "^production"],
      "options": {
        "command": "../../tools/tako-go.sh vet ./...",
        "cwd": "apps/backend",
        "env": {
          "GOCACHE": "/tmp/tako-go-cache",
          "TAKO_DASHBOARD_DIR": ""
        }
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go fmt ./...",
        "cwd": "apps/backend"
      }
    }
  }
}
