# Distribution packages should include their standard login stack here.
# Debian/Ubuntu: @include common-auth and common-account.
# Fedora/RHEL/Arch: include system-auth equivalents through package transforms.
auth required pam_unix.so
account required pam_unix.so
session required pam_unix.so
